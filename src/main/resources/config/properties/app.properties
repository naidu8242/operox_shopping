# Keep the environment specific properties in this file, and move/put all other common properties to config.properties file.

jdbc.driverClass=com.mysql.jdbc.Driver
jdbc.url=jdbc:mysql://127.0.0.1:3306/operox?useSSL=false
jdbc.username=root
jdbc.password=BIS@123

emailJobScheduler.job.cron.expression=0 30 19 ? * *
operoxUrl = http://localhost:8080/operox

# added a comment to test the git commit and push.